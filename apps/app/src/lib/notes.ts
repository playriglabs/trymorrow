/**
 * A note is a line someone reads next to an amount, not a letter: it has to fit one row on a
 * phone without pushing the date or the name around. Both the input and the API enforce it.
 */
export const MAX_NOTE = 30
